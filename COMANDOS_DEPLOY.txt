═══════════════════════════════════════════════════════════════
  COMANDOS EXACTOS PARA DESPLEGAR EN RENDER
═══════════════════════════════════════════════════════════════

PASO 1: GENERAR SECRET PARA NEXTAUTH
═══════════════════════════════════════════════════════════════
En PowerShell, ejecuta:

[Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))

Copia el resultado, lo necesitarás para NEXTAUTH_SECRET.


PASO 2: SUBIR CÓDIGO A GITHUB
═══════════════════════════════════════════════════════════════
Ejecuta estos comandos en orden:

git init
git add .
git commit -m "Listo para producción"
git remote add origin https://github.com/TU-USUARIO/rifa-altruista.git
git push -u origin main

IMPORTANTE: Cambia TU-USUARIO por tu nombre de usuario de GitHub.


PASO 3: CONFIGURAR EN RENDER.COM
═══════════════════════════════════════════════════════════════
1. Ve a: https://dashboard.render.com
2. Clic en "New +" → "Web Service"
3. Conecta tu repositorio de GitHub
4. Llena el formulario:

   Name: rifa-altruista
   Region: Oregon (US West)
   Branch: main
   Runtime: Node
   
   Build Command:
   npm install && npx prisma generate && npx prisma db push --accept-data-loss && npm run build
   
   Start Command:
   npm start
   
   Instance Type: Free

5. Agregar estas variables de entorno (una por una):

   DATABASE_URL
   postgresql://neondb_owner:npg_anbmFAE6V8eI@ep-little-dust-afway67t-pooler.c-2.us-west-2.aws.neon.tech/neondb?sslmode=require
   
   NEXTAUTH_URL
   https://TU-APP.onrender.com
   (reemplaza TU-APP con el nombre que te de Render)
   
   NEXTAUTH_SECRET
   (pega el secret que generaste en el PASO 1)
   
   NEXT_PUBLIC_TICKET_PRICE
   50
   
   NEXT_PUBLIC_DRAW_DATE
   2026-01-06T18:00:00
   
   NODE_VERSION
   18

6. Clic en "Create Web Service"


PASO 4: ESPERAR EL DEPLOY (10 minutos aprox)
═══════════════════════════════════════════════════════════════
Render te mostrará logs en tiempo real. Espera a que diga:
"Your service is live 🎉"

Copia la URL que te dio Render (ejemplo: https://rifa-altruista.onrender.com)


PASO 5: ACTUALIZAR NEXTAUTH_URL
═══════════════════════════════════════════════════════════════
1. En Render, ve a "Environment"
2. Edita NEXTAUTH_URL
3. Ponle la URL real que te dieron
4. Guarda (esto hará un nuevo deploy automático, espera 5 min)


PASO 6: INICIALIZAR BASE DE DATOS
═══════════════════════════════════════════════════════════════
En Render, ve a "Shell" (menú lateral) y ejecuta:

npx tsx prisma/seed.ts

Esto creará los 500 boletos.


PASO 7: CREAR USUARIO ADMIN
═══════════════════════════════════════════════════════════════
En la misma Shell de Render, ejecuta:

npx tsx scripts/crear-admin-rapido.ts

Usuario admin creado:
Email: admin@rifa.com
Password: Admin123!


PASO 8: PROBAR LA APP
═══════════════════════════════════════════════════════════════
1. Abre tu URL en el navegador
2. Verifica que cargue la página principal
3. Ve a /admin e inicia sesión con el admin
4. Cambia la contraseña del admin por seguridad


═══════════════════════════════════════════════════════════════
  ¡LISTO! TU APP ESTÁ EN PRODUCCIÓN
═══════════════════════════════════════════════════════════════

PARA ACTUALIZACIONES FUTURAS:
Cada vez que hagas cambios, solo ejecuta:

git add .
git commit -m "descripción del cambio"
git push

Render detectará el cambio y desplegará automáticamente (5-10 min).


URLS IMPORTANTES:
- Dashboard Render: https://dashboard.render.com
- Dashboard Neon: https://console.neon.tech
- Tu app: (la URL que te dio Render)
- Panel admin: (tu URL)/admin


SOPORTE:
Si algo falla, revisa:
1. Logs en Render Dashboard
2. Archivo GUIA_RENDER.md para más detalles
3. Variables de entorno estén correctas

